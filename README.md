# Wildfire-Burned-Area-Regression
Metaheuristic-optimized deep learning pipeline for predicting forest fire burned area (hectares) using the UCI Forest Fires dataset. Combines PyTorch MLP regression with SHAP-based feature selection and PSO hyperparameter tuning to beat a baseline model.
